import React from "react";
import { StyleSheet, Text, View } from "react-native";

interface Props {}

const TwoDAnimeInfo = (props: Props) => {
  return (
    <View>
      <Text>Title</Text>
    </View>
  );
};

export default TwoDAnimeInfo;

const styles = StyleSheet.create({});
