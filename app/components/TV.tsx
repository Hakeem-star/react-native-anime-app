import React from "react";
import { View, Text } from "react-native";

interface Props {}

const TV = (props: Props) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default TV;
